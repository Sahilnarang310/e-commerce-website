import React from 'react'
import { Button } from 'react-bootstrap'

const Body = (props) => {
  return (
    <div>
       <Button variant='primary'>click</Button>
    </div>
  )
}

export default Body